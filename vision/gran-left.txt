1. **Distinct card boxes: 3 stacked regions are visible**
   - **Top card:** approximately `y=0–335`. It has thin horizontal gray borders around the visible section, roughly `#68696d`; the side borders are cropped out. Corners appear **square**. No glow.
   - **Middle card:** approximately `y=336–648`. It is separated by a thin gray border, approximately `#626368`. Corners appear **square**. No glow.
   - **Bottom/table card:** starts around `y=649` and continues below the crop. It has a dark hairline boundary and a bright blue header band. Corners appear **square**. No glow.

2. **Steel-blue title bands:** **Not consistently.** The upper title area is dark slate gray, around `#33363d`, with light blue text. The lower card has a clearly filled steel-blue header band, approximately `#07547c` to `#0a5b86`, with a subtle lighter lower edge. It is the only strongly blue title/header band visible.

3. **Gaps between adjacent cards:** There is effectively **no open spacing gap**. The cards are stacked directly against each other, separated by thin horizontal rules. Where a small separation is visible, it shows the darker canvas/card background, approximately `#1b1b1b` or `#2d2e2e`, rather than a distinct padded gutter.

4. **Visible table:**
   - **Header band:** dark steel blue, approximately `#07547c` / `#0a5b86`.
   - **Row/body fill:** very dark charcoal, approximately `#292a2b` to `#303132`.
   - **Row separators:** subtle dark hairlines, approximately `#1f2021` or slightly lighter `#38393a`; they are low-contrast and not bright.
